# Roadmap

No dates, and nothing here is promised.

## Report shape

- A `--summary` mode printing only the counts, for dashboards and CI logs.
- Optional per-epoch grouping of the leader skip rates.

## Input

- A streaming mode for exports larger than memory.

## Verification

- A property test that runs parity over generated records,
  not only the authored fixtures.
